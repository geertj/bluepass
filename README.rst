Welcome to Bluepass!!

== Installation ==

Ubuntu based systems::

    # apt-get install python-dev cython python-qt4 libdbus-1-dev libssl-dev
    # pip install -r requirements.txt
    # python setup.py install


Fedora based systems::

    # yum install python-devel Cython PyQt4 dbus-devel
    # pip install -r requirements.txt
    # python setup.py install
